CREATE TABLE lista (
    matricula INT PRIMARY KEY,
    apellido VARCHAR(100),
    equipo VARCHAR(50)
);

CREATE TABLE empleados (
    nombre VARCHAR(100),
    departamento VARCHAR(50),
    salario NUMERIC
);
